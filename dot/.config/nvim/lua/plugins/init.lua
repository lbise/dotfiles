require('lazy').setup('plugins.configs', {
    defaults = {
        lazy = false,
    },
    --install = {
    --    colorscheme = {
    --        'tokyonight',
    --        'habamax'
    --    },
    --}
})
